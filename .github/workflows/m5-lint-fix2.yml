name: Milestone 5 lint remediation 2

on:
  pull_request:

permissions:
  contents: write

jobs:
  remediate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: feature-milestone-5
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: 24.15.0
          cache: npm
      - name: Apply second-pass lint remediation
        run: python3 scripts/m5-lint-remediate.py
      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint
      - run: npm run test:unit
      - run: git diff --check
      - name: Commit only verified remediation
        shell: bash
        run: |
          set -Eeuo pipefail
          rm scripts/m5-lint-remediate.py
          rm .github/workflows/m5-lint-fix.yml
          rm .github/workflows/m5-lint-fix2.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: satisfy milestone 5 lint contracts'
          git push origin HEAD:feature-milestone-5
